import React, { FunctionComponent } from 'react';
import { Link } from 'react-router-dom';

const MainPresenter: FunctionComponent = () => (
  <>
    <Link to="/issue/1">issue link</Link>
  </>
);

export default MainPresenter;
