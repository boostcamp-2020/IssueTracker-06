import createMilestone from './createMilestone';
import getMilestones from './getMilestones';
import updateMilestone from './updateMilestone';
import updateMilestoneStatus from './updateMilestoneStatus';
import deleteMilestone from './deleteMilestone';

export default { createMilestone, getMilestones, updateMilestone, updateMilestoneStatus, deleteMilestone };
